{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block content %}

    {% set header %}
        <h2 class="logo">MY APP</h2>
            <form action="{{ path('logout') }}" class="nav" method="post">
                <a class="nav_link">Translate to English</a>
                <button type="submit" name="logout" class="btn_nav" >Log out</button>
            </form>
    {% endset %}

    <div class="container">
        <section>
            <img class="avatar" src="http://s3.amazonaws.com/37assets/svn/765-default-avatar.png">
            <a class="like_button" name="edit" href="{{ path('edit_open', {'id': id}) }}">Edit</a>
        </section>
        <div class="profile_info">
            <form method="post">
                <p class="profile_label">Email: {{ email }}</p>
                <p class="profile_label">Birthday: {{ birthday }}</p>
                <p class="profile_label">Gender: {{ gender }}</p>
                <p class="profile_label">Country: {{ country }}</p>
                <button type="submit" name="delete" class="post_btn btn">delete this profile</button>
            </form>
        </div>
    </div>
{% endblock %}